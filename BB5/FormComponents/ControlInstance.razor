@using BB5.Models

@if (Template is not null
     && Model is not null)
{
    @Template(Model)
}

@code {
    [Parameter]
    public FormControlModel? Model { get; set; }

    [CascadingParameter]
    public RenderFragment<FormControlModel>? Template { get; set; }
}