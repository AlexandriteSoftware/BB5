namespace BB5;

public enum ValidationState
{
    None,
    Valid,
    Invalid
}

public enum Color
{
    None,
    Primary,
    Secondary,
    Success,
    Danger,
    Warning,
    Info,
    Light,
    Dark
}

public enum Shadow
{
    None,
    Small,
    Normal,
    Large
}

public enum ElementSize
{
    Normal,
    Small,
    Large
}

public enum ContentType
{
    Text,
    Html,
    Markdown
}
