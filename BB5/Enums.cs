namespace BB5;

public enum ValidationState
{
    None,
    Valid,
    Invalid
}

public enum TextBackgroundColor
{
    None,
    Primary,
    Secondary,
    Success,
    Danger,
    Warning,
    Info,
    Light,
    Dark
}

public enum Shadow
{
    None,
    Small,
    Normal,
    Large
}

public enum FormControlSize
{
    Default,
    Small,
    Large
}
