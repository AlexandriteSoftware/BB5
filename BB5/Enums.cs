namespace BB5;

public enum ValidationState
{
    None,
    Valid,
    Invalid
}

public enum AttentionState
{
    None,
    Primary,
    Secondary,
    Success,
    Danger,
    Warning,
    Info,
    Light,
    Dark
}

public enum ElementSize
{
    Normal,
    Small,
    Large
}

public enum ContentType
{
    Text,
    Html,
    Markdown
}
